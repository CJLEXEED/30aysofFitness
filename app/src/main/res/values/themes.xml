<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <!-- res/values/styles.xml -->
    <?xml version="1.0" encoding="utf-8"?>
    <resources>

        <!-- Light Theme -->
        <style name="AppTheme.Light" parent="Theme.Material3.Light.NoActionBar">
            <item name="colorPrimary">@color/colorPrimaryLight</item>
            <item name="colorOnPrimary">@color/black</item> <!-- Text/icons on primary color -->
            <item name="colorSecondary">@color/colorSecondaryLight</item>
            <item name="colorOnSecondary">@color/black</item>
            <item name="android:background">@color/white</item> <!-- Background color -->
            <item name="android:textColor">@color/textColorLight</item> <!-- Text color -->
            <item name="android:windowBackground">@color/white</item>
        </style>

    </resources>


    <style name="Theme._30DaysOfFitness" parent="Base.Theme._30DaysOfFitness" />
</resources>